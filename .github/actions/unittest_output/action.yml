# This file makes github actions understand that this is an action that should be ran with the defined Dockerfile
name: "unittest_output"
description: "Create a unit test for checking whether all required columns are in our output data"
author: "arthur.huys@student.vives.be"

runs:
  using: "docker"
  image: "Dockerfile"